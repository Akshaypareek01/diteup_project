/** Desktop hero frame — matches `aspect-[1774/887]` on the homepage. */
export const DESKTOP_BANNER_WIDTH = 1774;
export const DESKTOP_BANNER_HEIGHT = 887;

/** Mobile hero artwork — full-bleed portrait on phones. */
export const MOBILE_BANNER_WIDTH = 870;
export const MOBILE_BANNER_HEIGHT = 1808;

export const MAX_HOMEPAGE_BANNERS = 8;

export const DESKTOP_BANNER_SPEC = `${DESKTOP_BANNER_WIDTH} × ${DESKTOP_BANNER_HEIGHT} px (2:1 landscape)`;
export const MOBILE_BANNER_SPEC = `${MOBILE_BANNER_WIDTH} × ${MOBILE_BANNER_HEIGHT} px (portrait)`;
