/**
 * Values referenced by storefront UX and policy pages — update both if business rules change.
 */
export const FREE_SHIPPING_THRESHOLD_INR = 499;

/** Calendar days from delivery for eligible return requests (see refund policy). */
export const RETURN_WINDOW_DAYS = 7;
